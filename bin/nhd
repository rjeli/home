#!/bin/bash
set -euo pipefail

nh darwin build --diff=always

attrBrewfile="darwinConfigurations.rj-m4.config.homebrew.brewfile"
oldBrewfile=$(cat /etc/Brewfile)
newBrewfile=$(nix eval --raw "$NH_DARWIN_FLAKE#$attrBrewfile")
difft <(echo "$oldBrewfile") <(echo "$newBrewfile")
